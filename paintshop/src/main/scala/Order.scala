/**
  * Represents a single order in the order file.
  *
  * @param colour the colour that was ordered.
  * @param paintType the type of paint that was ordered.
  */
case class Order(colour: Int, paintType: String)
